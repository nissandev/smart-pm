# Backend API base URL (default: /api via nginx proxy)
VITE_API_URL=/api

# Sentry DSN for error tracking (leave empty to disable)
VITE_SENTRY_DSN=

# Show demo login buttons on the login page (true in dev by default, false in production)
VITE_SHOW_DEMO=false
